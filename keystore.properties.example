# Скопируйте в keystore.properties (файл в .gitignore, в Git не попадает)
# и заполните реальными значениями. Как создать keystore — см. README, раздел «Release-подпись».
storeFile=/absolute/path/to/menstrual-calendar-release.jks
storePassword=CHANGE_ME
keyAlias=release
keyPassword=CHANGE_ME
